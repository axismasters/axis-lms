// AXIS LMS v1.2 - TeacherVideos (Phase 3D v3: "수업자료"로 통합됨)
// 이 화면은 TeacherMaterials.tsx(수업영상/수업노트 탭 통합)로 대체되었다.
// TeacherRoutes.tsx가 /teacher/videos를 /teacher/materials?tab=videos로 리다이렉트하므로
// 이 컴포넌트는 어떤 라우트에도 연결되어 있지 않다(실수로 import되어도 안전한 최소 stub).

export default function TeacherVideos() {
  return null;
}
